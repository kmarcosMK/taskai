import { posts } from '../../lib/posts'; export default function Post({post}){return <div className='container section'>Post</div>} export async function getStaticPaths(){ const paths = posts.map(p=>({params:{slug:p.slug}})); return {paths,fallback:false}} export async function getStaticProps({params}){ const post = posts.find(p=>p.slug===params.slug)||null; return {props:{post}} }